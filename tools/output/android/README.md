# Android Design Tokens

Auto-generated by `tools/sync-design-tokens.swift` from `docs/design-tokens.json`.

## Files

| File | Description |
|------|-------------|
| `ThemeTokens.kt` | Jetpack Compose theme object |
| `res/values/colors_tokens.xml` | Light mode colors |
| `res/values-night/colors_tokens.xml` | Dark mode colors |
| `res/values/dimens_tokens.xml` | Spacing, corner radius, icon sizes |

## Usage (Jetpack Compose)

```kotlin
import com.webbridgekit.design.ThemeTokens

// Colors (auto-adapts to dark mode)
val bgColor = ThemeTokens.Color.background

// Spacing
val padding = ThemeTokens.Spacing.md // 12.dp

// Typography
val titleStyle = ThemeTokens.Typography.screenTitle
```

## Usage (XML Resources)

```xml
<ImageView
    android:padding="@dimen/spacing_md"
    android:background="@color/primary" />
```

## Regeneration

Run from project root:
```bash
bash tools/sync-tokens.sh
```
